/**
 * EMAGE GALAXY - Reusable Component Templates
 * Reference file for shared UI component structure.
 * Components are dynamically injected via components.js
 */

const COMPONENT_TEMPLATES = {
  navbar: 'Injected via Components.injectNavbar()',
  footer: 'Injected via Components.injectFooter()',
  wallpaperModal: 'Created dynamically via WallpaperGallery.openDetailModal()',
  welcomePopup: 'Created via Components.initWelcomePopup()',
  pageLoader: 'Created via Components.initLoader()',
  scrollProgress: 'Created via Components.initScrollProgress()',
  scrollTop: 'Created via Components.initScrollTop()'
};
